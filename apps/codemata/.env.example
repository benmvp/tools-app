# Google AI API Key (required for AI-generated content)
GOOGLE_API_KEY=your_google_ai_api_key_here
GEMINI_MODEL="gemini-3-flash-preview"

# Google Analytics (GA5) Measurement ID (optional, for production tracking)
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-ABCDE12345"

# ==============================================================================
# Local Development URL Configuration
# ==============================================================================
# In production, Vercel automatically sets VERCEL_PROJECT_PRODUCTION_URL
# For local development, set it to localhost with the port number
VERCEL_PROJECT_PRODUCTION_URL="localhost:3001"

# ==============================================================================
# AI Generation Strategy (Convention-Based)
# ==============================================================================
# The app automatically detects the environment and configures AI behavior:
#
# Scenarios (no configuration needed):
# 1. Local dev (`next dev`)
#    → AI disabled automatically (saves API quota during hot-reload)
#
# 2. Local production build (`next build && next start`)
#    → Full production mode (pre-render all pages, enable prefetch)
#
# 3. Vercel preview (PR deployments)
#    → Preview mode automatically (on-demand generation, no prefetch)
#    → Vercel sets VERCEL_ENV=preview automatically
#
# 4. Vercel production
#    → Full production mode automatically (pre-render, prefetch)
#    → Vercel sets VERCEL_ENV=production automatically
#
# Override/Testing scenarios (uncomment to use):
# ------------------------------------------------------------------------------
# Enable AI in local dev (on-demand generation, no prefetch):
# VERCEL_ENV="preview"
# NEXT_PUBLIC_VERCEL_ENV="preview"
#
# Force disable AI in any environment (pages still pre-render, just without AI):
# DISABLE_AI="true"
#
# Note: Local production builds (next build) automatically pre-render with AI.
# No need to set VERCEL_ENV="production" - it's the default for next build.
